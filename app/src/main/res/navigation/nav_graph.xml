<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_fragment">

    <fragment
        android:id="@+id/home_fragment"
        android:name="com.example.javichordskotlin.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/home_to_song_list"
            app:destination="@id/song_list_fragment" />
        <action
            android:id="@+id/home_to_add_song"
            app:destination="@id/add_song_fragment" />
    </fragment>

    <fragment
        android:id="@+id/song_list_fragment"
        android:name="com.example.javichordskotlin.ui.songList.ui.SongListFragment"
        android:label="Song List"
        tools:layout="@layout/fragment_song_list">
    </fragment>

    <fragment
        android:id="@+id/add_song_fragment"
        android:name="com.example.javichordskotlin.ui.addSong.AddSongFragment"
        android:label="Add Song"
        tools:layout="@layout/fragment_add_song">
    </fragment>
</navigation>